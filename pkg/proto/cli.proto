syntax = "proto3";

package api;

service CLI {
    rpc Status (EmptyMessage) returns (StatusResponse) {}
}

message EmptyMessage {}

message StatusResponse {
    int64 health = 1;
    int64 state = 2;
}