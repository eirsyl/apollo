syntax = "proto3";

package api;

service Manager {
    rpc AgentHealth (HealthRequest) returns (HealthResponse) {}
}

message HealthRequest {
    string nodeId = 1;
    bool ready = 2;
    string detail = 3;
}

message HealthResponse {
    bool ack = 1;
}
