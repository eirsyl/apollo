syntax = "proto3";

package api;

service Manager {
    rpc AgentHealth (stream HealthRequest) returns (stream HealthResponse) {}
}

message HealthRequest {
    string instanceId = 1;
}

message HealthResponse {
    bool ack = 1;
}
